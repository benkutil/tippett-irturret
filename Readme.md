# Tippett's HackPack IR Turret Setup
## Customizations
- Custom password
- Add laser module, trigger with `0`
- Add distance sensor. Trigger tracking with `1`, and fire if distance of object is within threshold